# === SECRETS — never commit these ===
# Google Places API key lives here, local-only. See places-api-integration.md.
gcloud.secret
*.secret
.env
.env.*

# Node
node_modules/

# OS cruft
.DS_Store
Thumbs.db
fetch-progress.log

# Local verification artifacts
sheet-verification.png
.playwright-mcp/

# GitHub token for half4u (local only)
half4u.token
*.token
