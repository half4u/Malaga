# === SECRETS — never commit these ===
# Google Places API key lives here, local-only.
gcloud.secret
*.secret
.env
.env.*

# Node
node_modules/

# OS cruft
.DS_Store
Thumbs.db
fetch-progress.log

# Local verification artifacts
sheet-verification.png
.playwright-mcp/

# GitHub token for half4u (local only)
half4u.token
*.token

# Dev artifacts — kept local, not shipped to the repo
docs/
fetch-places.mjs
places-api-integration.md

